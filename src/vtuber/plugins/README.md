# Built-in Plugins

This directory contains built-in plugins that ship with vtuber.

On first startup, these plugins are copied to `~/.vtuber/plugins/` so the
agent can load them. Users can also place their own plugins in that directory.

## Plugin Format

Each subdirectory is a Claude Code plugin. See the Claude Agent SDK
documentation for the plugin directory structure.
